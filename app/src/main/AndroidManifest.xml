<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.valevich.clean"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:name="com.valevich.clean.UmoriApplication"
        android:supportsRtl="true"
        android:theme="@style/Dark">

        <activity
            android:name="com.valevich.clean.presentation.ui.activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".presentation.ui.activities.StoriesWrapperActivity"/>

    </application>

</manifest>